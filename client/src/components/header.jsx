import {Typography} from '@mui/material'

export const Header =() => {
  

    return (
           
        <div><Typography variant='h1' style={{display:'flex', justifyContent:'center', color:'blue', marginTop:'20px'}}>ToDo List</Typography></div>

    )
}

export default Header